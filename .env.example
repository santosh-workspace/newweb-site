# Public site URL — used for canonical URLs, sitemap, Open Graph
NEXT_PUBLIC_SITE_URL=https://santoshnagargoje.dev

# Optional: contact form endpoint (e.g. Formspree / AWS API Gateway + Lambda)
NEXT_PUBLIC_CONTACT_ENDPOINT=
